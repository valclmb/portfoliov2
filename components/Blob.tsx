import clsx from "clsx";

export const Blob = () => {
  return (
    <div
      className={clsx(
        "absolute right-20 top-5 rounded-full size-80 z-50 bg-violet-500 blur-[300px] animate-scale"
      )}
    ></div>
  );
};
